name: docker-reverse-proxy

services:
  reverse-proxy:
    build: .
    command: npm run dev
    container_name: reverse-proxy
    ports:
      - 80:80
      - 8080:8080
    network_mode: bridge
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app
